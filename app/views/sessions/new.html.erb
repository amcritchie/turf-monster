<%# Canonical /signin page (local override of the engine login page). Login and
    signup are one create-or-login flow, so this renders the single unified auth
    card. /login + /signup redirect here (see config/routes.rb). %>
<%= render "shared/auth_card" %>
