<%# Cropper.js (CSS + JS) for the shared crop-photo modal. Rendered on pages
    that can open the cropper — the account avatar, the contest banner editor,
    and the profile / signup avatar field — so the ~40 KB only loads where an
    upload trigger exists rather than globally in the layout. %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.css">
<script defer src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.js"></script>
