<%# Local override of the engine signup page. /signup now 301-redirects to
    /signin (config/routes.rb), so this only renders on a POST /signup create
    failure — it shows the same unified auth card as /signin for consistency. %>
<%= render "shared/auth_card" %>
