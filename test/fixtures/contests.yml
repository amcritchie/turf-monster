one:
  name: Test Contest
  slug: test-contest
  entry_fee_cents: 1900
  status: open
  max_entries: 29
  contest_type: standard
  # Relative so the contest never falls past its lock time as real dates pass
  # (was 2026-06-11, which started failing EntryTest once that date arrived).
  starts_at: <%= 2.years.from_now.strftime("%Y-%m-%d %H:%M:%S") %>
  slate: one
  rank: 100
  user: alex
